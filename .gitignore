# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Test coverage output (vitest)
coverage

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

/app/generated/prisma

# Claude Code agent memory (machine-specific runtime data)
.claude/agent-memory/
.claude/worktrees/
.claude/scheduled_tasks.lock

# Third-party agent skills (installed locally, not tracked)
.agents/
.vercel
.env*

# Licensed exercise media (MoveKit) — never committed; see docs/licenses/movekit.md.
# Root-anchored: an unanchored "media/" would also swallow scripts/media/.
/media/
# Storage index with the random path tokens (see docs/licenses/movekit.md). Back it
# up with the raw source files; media-manifest.json (the ledger) stays committed.
/media-manifest.private.json
